version: 2
updates:
  - package-ecosystem: pip
    directory: /backend
    schedule:
      interval: weekly

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # npm entry deliberately omitted — no frontend/ exists yet (Phase 1 is
  # backend-only). Add one once the frontend is built.
